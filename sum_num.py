# Initialize the variable `n` to store the sum of numbers
n=0   

# Loop from 0 to 10 (inclusive) using the range function
for i in range(11):

# Add the current value of `i` to `n`
   n=n+i
# Print the result after the loop ends

print ("sum of first 10 natural numbers",n)  
